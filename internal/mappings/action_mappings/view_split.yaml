mappings:
  - id: "actions.view.focusNextSplit"
    short_description: "Focus next split"
    description: "Focus next editor split"
    vscode:
      command: "workbench.action.focusNextGroup"
      when: "editorFocus"
    intellij:
      action: "NextSplitter"
    zed:
      action: "workspace::ActivateNextPane"
      context: "Editor"
    helix:
      command: "rotate_view"
      mode: "insert"
  - id: "actions.view.focusPreviousSplit"
    short_description: "Focus previous split"
    description: "Focus previous editor split"
    vscode:
      command: "workbench.action.focusPreviousGroup"
      when: "editorFocus"
    intellij:
      action: "PrevSplitter"
    zed:
      action: "workspace::ActivatePreviousPane"
      context: "Editor"
    helix:
      command: "rotate_view_reverse"
      mode: "insert"
# TODO split right
