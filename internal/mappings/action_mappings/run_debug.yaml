mappings:
  - id: "actions.run.startDebugging"
    short_description: "Start debugging"
    description: "Start Debugging"
    vscode:
      command: "workbench.action.debug.run"
      when: "debuggersAvailable && debugState == 'inactive'"
    zed:
      action: "debugger::Start"
    intellij:
      action: "Debug"
    helix:
      command: "dap_launch"
      mode: "insert"
  - id: "actions.run.stopDebugging"
    short_description: "Stop debugging"
    description: "Stop Debugging"
    vscode:
      command: "workbench.action.debug.stop"
      when: "inDebugMode && !focusedSessionIsAttach"
    zed:
      action: "debugger::Stop"
    intellij:
      action: "Stop"
    helix:
      command: "dap_terminate"
      mode: "insert"
  - id: "actions.run.restartDebugging"
    short_description: "Restart debugging"
    description: "Restart Debugging"
    vscode:
      command: "workbench.action.debug.restart"
      when: "inDebugMode"
    zed:
      action: "debugger::RerunSession"
      context: ""
    intellij:
      action: "Rerun"
    helix:
      command: "dap_restart"
      mode: "insert"
